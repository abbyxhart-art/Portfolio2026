// Shared with Home.tsx so the pinned "Abby Hart" name plate and LinkedIn
// button track the nav's resting/scrolled top offset exactly.
export const NAV_TOP_REST = 32;
export const NAV_TOP_SCROLLED = 16;
